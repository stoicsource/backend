api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    http_cache:
        public: true
    defaults:
        pagination_enabled: false
        cache_headers:
            max_age: 86400
            vary: [ 'Content-Type' ]
